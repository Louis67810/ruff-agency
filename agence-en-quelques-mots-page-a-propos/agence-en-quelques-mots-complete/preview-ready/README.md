# Section « L’agence en quelques mots » — React optimisé

Le composant est indépendant du runtime Framer.

## Dépendances applicatives
- `react`
- `framer-motion` (uniquement pour les animations texte + hover CTA réellement présentes dans l’export)

## Utilisation
```jsx
import SectionAgenceEnQuelquesMots from "./SectionAgenceEnQuelquesMots";
import "./SectionAgenceEnQuelquesMots.css";

<SectionAgenceEnQuelquesMots callHref="/contact" />
```

Le lien original du CTA était l’ID de page Framer `XvvJHap2q`; aucune URL publique n’a été inventée.
